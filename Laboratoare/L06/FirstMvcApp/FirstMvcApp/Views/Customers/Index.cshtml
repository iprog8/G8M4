@model FirstMvcApp.ViewModels.CustomerIndexViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    //bag ce cod C# vrea muschiu meu
    //intrebarea e ... Vreau? NU!
    int a = 7;
    var abcd = "bere";
    //aici putem scrie DOAR cod C#
}

<h2>Index</h2>

@a
@abcd

@if (abcd is string)
{
    <p>berea e string</p>
}
else
{
    <p>Berea nu stuy ce e   </p>
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" style="color:darkred">
    <tr>
        <th>
            FullName
        </th>
        <th>
           Country
        </th>
        <th>
            Phone
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.CustomerList)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Country)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.ActionLink("Edt", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }
@a
</table>
@for (int i =1; i<=Model.PageInfo.MaxPage;i++)
{
    @Html.ActionLink(i.ToString(), "Index", new { page = i});

<span>&nbsp;</span>

}

@Html.Partial("_Pagging", Model.PageInfo)